import Config from "./Config";

export default class Waf {
  constructor(config: Config) {
    if (config.path) {
    }
    console.log(config);
  }
  private parseConfig(): void {
    
  }
}
